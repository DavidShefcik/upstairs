import { Outlet, RouteProps } from "react-router-dom";

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default function SettingsLayout(props: RouteProps) {
  return <Outlet />;
}
